-- The select command
--Its table outlook or columns names
CREATE TABLE earthquake
(
	earthquake_id,
	occurred_on,
	latitude,
	longitude,
	depth,
	magnitude,
	calculation_method,
	network_id,
	place,
	cause
);